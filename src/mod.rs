/**
 * Module entry point for shared command definitions.
 *
 * This file re-exports the `commands` module so other parts of the project can
 * import the core AST types from a single place when needed.
 */
pub mod commands;
